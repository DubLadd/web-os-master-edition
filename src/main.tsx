import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../web_os_master_edition'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
